package com.fredrikw.fructose.structs;

/**
 * A stack that doesn't allow duplicates.
 * 
 * @author Fredrik
 *
 * @param <T> - The item data type
 */
public interface SetStack<T> extends Stack<T> {
	
}
